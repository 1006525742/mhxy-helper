# 分图助手 · 使用说明

> 分图助手（宝图助手）用于《梦幻西游》宝图场景：自动识别藏宝图展开图上印的**地图名**，并在物品栏格子内提示该图**应当存入第几个仓库**，省去手动记忆「哪张图放哪个仓库」的麻烦。

---

## 一、环境启动

| 组件 | 地址 / 端口 | 说明 |
|---|---|---|
| 前端 | http://localhost:3000 | Vite dev server |
| 后端 | http://localhost:8002（路由前缀 `/api/baotu`） | FastAPI，由 macOS LaunchAgent 托管 |

### 1. 前端
```bash
cd frontend
npm run dev      # 打开 http://localhost:3000
```

### 2. 后端
后端由 LaunchAgent（`com.mhxy.baotu.plist`）**开机自启 + 崩溃自动重启**，正常情况下无需手动启动。

> ⚠️ **改了后端 Python 代码后，必须用下面命令重启，才会生效：**
> ```bash
> launchctl kickstart -k gui/501/com.mhxy.baotu
> ```
> （`501` 是当前用户 uid，可用 `id -u` 查询。请勿用 `nohup` 直接启动——会跟 LaunchAgent 抢 8002 端口导致启动失败。）

### 3. 权限
- 实时识别需要**屏幕录制/共享权限**（浏览器 `getDisplayMedia`）。首次共享时在系统弹窗中允许。
- 若不想共享屏幕，可用界面上的「📷 上传截图测试」按钮，直接选一张藏宝图展开图来识别。

---

## 二、界面三大区块

页面整体水平居中，最大宽 1388px，分为：

1. **顶部卡片**：清空 / 上传截图测试 / 识别位置模式
2. **物品栏网格**（左侧）：20 格，显示识别结果
3. **仓库宝图存放设置**（右侧）：配置每个仓库对应哪张地图

---

## 三、物品栏格子怎么看

物品栏共 20 格（4 行 × 5 列），对应游戏内物品栏排布。

| 状态 | 显示 |
|---|---|
| **已识别** | 居中显示**大数字**（=该地图对应的仓库槽位号），数字颜色为该地图的默认色；数字下方显示**地图名**（中性深色，不染色） |
| **未识别** | 干净的空槽，无数字、无噪点 |

**读法示例**：某一格显示绿色大数字「2」+「花果山」→ 这张宝图应存入**仓库 2（花果山）**。

> 数字和颜色都来自「仓库宝图存放设置」里该地图所绑定的仓库槽位，因此物品栏与右侧仓库面板颜色一致。

---

## 四、仓库宝图存放设置

右侧面板用于配置「每张地图 → 第几个仓库」。

### 默认映射（16 个仓库）
| 仓库 | 地图 | 仓库 | 地图 |
|---|---|---|---|
| 1 | 傲来国 | 9 | 五庄观 |
| 2 | 花果山 | 10 | 西凉女国* |
| 3 | 女儿村 | 11 | 朱紫国 |
| 4 | 宝象国 | 12 | 北俱芦洲 |
| 5 | 长寿村 | 13 | 长寿郊外 |
| 6 | 大唐境外 | 14 | 大唐国境 |
| 7 | 建邺城 | 15 | 麒麟山 |
| 8 | 江南野外 | 16 | 狮驼岭 |

> *宝图场景实际不含「西梁女国」，该默认条目可忽略或在下拉中改为其它图。墨家村、东海湾等图若需要可自行在扩展仓库中指定。

### 颜色含义
- 徽章**灰色** = 该仓库尚未选择地图。
- 选择地图名后，徽章**自动填充该地图的默认色**（与物品栏大数字颜色一致）。

### 扩展与删除
- 点「**＋ 添加仓库**」可新增仓库，最多 **40 个**（17~40 为扩展槽，初始为空、无默认地图）。
- 第 17 个及以后的仓库，每行右侧有「×」可删除。
- 前 16 个默认仓库不可删除，以保证默认映射稳定。

### 恢复默认设置
- 面板标题右侧有「**恢复默认设置**」按钮。
- 点击会二次确认，确认后清空浏览器中所有仓库配置，回到 **16 个默认仓库**（傲来国=1、花果山=2…）。

### 持久化
- 所有仓库设置保存在**浏览器 `localStorage`**（`scene_0`~`scene_39` + `warehouse_slot_count`）。
- 刷新页面、关闭重开浏览器、重启电脑都**保留**。
- 换浏览器 / 开无痕模式会读不到，回落默认。
- 应用无登录/用户系统，无法在同一浏览器内区分多个游戏角色。

---

## 五、两种识别方式

### 方式 A：屏幕共享实时识别
1. 点界面上的共享/识别按钮，选择游戏窗口。
2. 后端持续截取物品栏与藏宝图区域，OCR 识别地图名并实时填到物品栏格子。
3. 浮窗可「»」收起到侧边栏，避免遮挡。

### 方式 B：上传截图测试（免共享屏幕）
1. 点「📷 上传截图测试」。
2. 选择一张藏宝图展开图（含地图名那张）。
3. 后端返回识别结果并填到物品栏对应格，同时显示裁剪预览，便于核对截取是否正确。

---

## 六、识别位置模式

顶部「识别位置」可切换物品栏在屏幕上的位置布局，以贴合不同分辨率/窗口摆放：
- **横向**（默认）
- **左仓库** / **右仓库**（针对仓库界面在左/右时的物品栏排布）

切换模式会重置当前网格识别结果。

---

## 七、OCR 识别说明

- 分图助手**只识别地图名**，坐标可选：即使坐标被误识别成 (0,0)，只要地图名认到就会正常填格子。
- 内置 18 张宝图地图，并预置常见 OCR 变体（识别引擎偶尔把字认错时的兜底）：

| 标准地图 | 已覆盖的 OCR 变体 |
|---|---|
| 傲来国 | 傲莱国 |
| 宝象国 | 玉象国 |
| 大唐境外 | 天唐境外 |
| 建邺城 | 建邮城、邮城 |
| 女儿村 | 安儿村、安几村 |
| 朱紫国 | 朱华国 |
| 大唐国境 | 大唐国 |

> 若某张图被误识别成别的地图名，把后端日志里对应的 `[Fentu OCR]` 那行发给开发者，补充对应变体即可。

---

## 八、日志与排错

| 内容 | 位置 |
|---|---|
| 后端运行日志（OCR 详细过程） | `backend/logs/baotu.log`（LaunchAgent 接管后同时写 `/tmp/backend8002.log`） |
| 后端健康 | `curl http://localhost:8002/api/baotu/health` 应返回 200 |

**常见问题**
- **改了后端不生效**：见上方「环境启动 → 后端」的重启命令（必须 `launchctl kickstart`，不能 `nohup`）。
- **物品栏不填格子**：确认截图/共享的是「藏宝图展开图」（带地图名那张），而非未展开的藏宝图图标。
- **仓库设置刷新后丢失（17~40 槽）**：旧版本有该 bug，已修复；若仍在旧代码请更新后重载。

---

## 九、一句话工作流

> 打开页面 →（共享屏幕 或 上传截图）→ 物品栏格子自动显示「第几仓库 + 地图名 + 颜色」→ 按提示把宝图存入对应仓库 → 仓库映射在右侧面板维护，需要时「恢复默认设置」一键重置。
