# 新增模块外网接入手册（mhxy-helper）

> 目标：让新做的「本地后端 + 前端页面」能通过 `https://yjmhxy.top` 被外网访问。
> 一句话本质：**你的服务在 Mac 上，外网看到的是 VPS**。中间靠 **frpc 隧道** 把 Mac 端口搬到 VPS，再用 **Nginx** 按 URL 路径分流。

---

## 一、链路架构

```
浏览器
  │  https://yjmhxy.top  (Let's Encrypt 证书)
  ▼
VPS Nginx:443  (117.72.108.169)
  │  按 URL 路径匹配 location，反代到 127.0.0.1:<remotePort>
  ▼
VPS frps  (remotePort, 如 8090)          ← 由 frpc 建立隧道后才会 LISTEN
  ▲
  │  frpc 隧道（出站连接，无需 VPS 开新端口）
  │
Mac 127.0.0.1:<localPort>  (如 8008)     ← 你的本地后端
  ▲
  │  uvicorn / FastAPI
前端 Vite dev server :3000               ← 生产就是它在 serve（不是 build 产物）
```

**四层必须都齐，缺一层就断**：

| 层 | 组件 | 出问题时的现象 |
|---|---|---|
| 1 | 本地后端在跑 | 502 / 连接被拒 |
| 2 | frpc 隧道已建立 | VPS remotePort 不监听 → 502 |
| 3 | Nginx 有对应 location | 404（落到 `/api/` 兜底到 ghost）|
| 4 | 前端调用路径一致 | 前端报错 / 请求打错后端 |

---

## 二、端口分配表（当前已占用，新模块往后排）

| 服务 | Mac 本地端口 | VPS remotePort | Nginx location |
|---|---|---|---|
| 前端 Vite | 3000 | 8080 | `/`、`/mhxy/static/*`、`/models/*` |
| 抓鬼 ghost | 8000 | 8081 | `/api/`（**兜底**）、`/api/ghost/*` |
| 科举 keju | 8001 | 8082 | `/api/ocr`、`/api/WatuOCR` |
| 宝图 baotu | 8002 | 8083 | `/api/baotu`、`/api/fentu` |
| 挖图 watu | 8003 | 8084 | `/api/watu` |
| 精灵 sprite | 8004 | 8086 | `/api/sprite` |
| 慈心 cixin | 8005 | 8087 | `/api/othello` |
| 藏宝阁 cbg | 8006 | 8089 | `/api/cbg` |
| 收货 harvest | 8007 | 8091 | `/api/harvest` |
| 答题 dati | 8008 | 8090 | `/api/dati` |
| 金价 gold | 8010 | 8088 | `/api/gold` |
| 物价 wuji | 8767 | 8085 | 子域名 `wuji.yjmhxy.top` |

> **新模块建议**：本地端口从 **8011** 起，remotePort 从 **8092** 起（先查占用，见步骤 0）。

---

## 三、接入五步

### 步骤 0 · 选端口

```bash
# 查候选端口是否空闲
for p in 8011 8012 8013; do
  lsof -nP -iTCP:$p -sTCP:LISTEN >/dev/null 2>&1 && echo "$p 已占用" || echo "$p 空闲"
done
# 再挑一个没人用的 remotePort
for p in 8092 8093 8094; do
  ssh root@117.72.108.169 "ss -tln | grep -q ':$p' && echo '$p 已占用' || echo '$p 空闲'"
done
```

### 步骤 1 · 本地起后端 + launchd 守护

新建 `~/Library/LaunchAgents/com.mhxy.<name>.plist`（照抄下面模板，改 4 处：Label / 脚本名 / 端口 / 日志名）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.mhxy.NAME</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Users/zhy/.workbuddy/binaries/python/envs/mhxy-backend/bin/python3</string>
        <string>-m</string>
        <string>uvicorn</string>
        <string>backend_NAME:app</string>
        <string>--host</string>
        <string>0.0.0.0</string>
        <string>--port</string>
        <string>80XX</string>
    </array>

    <!-- 必须指向 backend 目录，否则相对路径资源（题库/模型）解析失败 -->
    <key>WorkingDirectory</key>
    <string>/Users/zhy/mhxy-helper/backend</string>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/zhy/mhxy-helper/backend/logs/NAME.stdout.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/zhy/mhxy-helper/backend/logs/NAME.stdout.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>
</dict>
</plist>
```

加载并启动（**在你自己的 Mac 终端执行**，WorkBuddy 沙箱连不上 launchd GUI 域）：

```bash
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.mhxy.NAME.plist
launchctl kickstart -k gui/$(id -u)/com.mhxy.NAME
lsof -nP -iTCP:80XX -sTCP:LISTEN        # 确认在听
```

> 后端用的 venv 是 `mhxy-backend`（`/Users/zhy/.workbuddy/binaries/python/envs/mhxy-backend`）。
> 若依赖系统 `/usr/bin/python3`（如 cv2 4.13.0 相关），按需换成系统解释器。

### 步骤 2 · 加 frpc 隧道

编辑 `/Users/zhy/WorkBuddy/2026-07-03-22-49-14/frp/frpc.toml`（这就是 `com.mhxy.frpc` 实际读取的文件），在末尾追加：

```toml
[[proxies]]
name = "mhxy-NAME"
type = "tcp"
localIP = "127.0.0.1"
localPort = 80XX
remotePort = 809X
```

### 步骤 3 · 加 Nginx 路由并推送

编辑 `/Users/zhy/WorkBuddy/2026-07-03-22-49-14/mhxy_nginx_new.conf`，
**必须加在兜底 `location /api/` 之前**（否则会被 ghost 抢先匹配）：

```nginx
    # <模块中文名> -> NAME 后端 (809X，frpc 隧道 80XX->809X)
    location /api/NAME {
        proxy_pass http://127.0.0.1:809X;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # 上传图片/大文件时才需要下面两行
        # client_max_body_size 20m;
        # proxy_read_timeout 120s;
    }
```

推送（一条命令搞定备份→上传→`nginx -t`→reload）：

```bash
/Users/zhy/.workbuddy/binaries/python/envs/vps/bin/python \
  /Users/zhy/WorkBuddy/2026-07-03-22-49-14/push_nginx.py
```

### 步骤 4 · 前端调用 + vite proxy

1. 前端**用相对路径**调后端（不要写死 `localhost:80xx`）：
   ```ts
   const res = await fetch('/api/NAME/xxx', { method: 'POST', body: fd })
   ```
2. 本机想直连调试的话，在 `frontend/vite.config.ts` 的 api-proxy 分发里加一条前缀 → 端口映射
   （该插件把 `/api/NAME` 转发到 `127.0.0.1:80XX`，与线上 Nginx 行为保持一致）。

### 步骤 5 · 重启 frpc + 验证

```bash
# 在 Mac 终端执行（会重启 frpc，让新隧道生效）
bash ~/mhxy-helper/restart_mhxy.sh
```

外网验证：

```bash
curl -sk -w '\nHTTP=%{http_code}\n' https://yjmhxy.top/api/NAME/xxx
```

---

## 四、验证清单（按层排错）

```bash
# 1) 本地后端在跑
lsof -nP -iTCP:80XX -sTCP:LISTEN

# 2) VPS remotePort 已监听（frpc 已生效）
ssh root@117.72.108.169 "ss -tln | grep :809X"

# 3) Nginx 路由已存在（在 VPS 上）
ssh root@117.72.108.169 "grep -n 'api/NAME' /etc/nginx/sites-available/mhxy"

# 4) 外网实测
curl -sk -o /dev/null -w 'HTTP=%{http_code}\n' https://yjmhxy.top/api/NAME/xxx
```

返回码含义：

| 返回码 | 含义 |
|---|---|
| 200 | ✅ 全通 |
| 422 | ✅ 路由已通（FastAPI 参数校验失败，说明请求到了后端）|
| 404 | ❌ Nginx 缺 location，落到 `/api/` 兜底到 ghost |
| 502 | ❌ frpc 隧道未生效（VPS 端口没监听）或本地后端没起 |
| 000 | ❌ 连接失败（域名/证书/网络问题）|

---

## 五、常见问题

| 现象 | 根因 | 解决 |
|---|---|---|
| 外网 404 | Nginx 无该 location → 落 `/api/` 兜底 ghost | 步骤 3 补 location 并推送 |
| 外网 502 | frpc 没重启，VPS remotePort 未监听 | `bash ~/mhxy-helper/restart_mhxy.sh` |
| 外网 502 | 本地后端没起 | 查 launchd / `lsof`，kickstart |
| 本机通、外网不通 | 前端硬编码 `localhost:80xx` | 改相对路径 `/api/NAME/...` |
| 改了 frpc.toml 不生效 | frpc 未重启 | 重启 frpc |
| 改了 Nginx 不生效 | 没推到 VPS | 跑 `push_nginx.py` |
| Mac 重启后失效 | 没配 launchd | 步骤 1 建 plist |

---

## 六、注意事项

1. **沙箱限制**：WorkBuddy 沙箱**不能**重启 frpc / 操作 launchd（GUI 域隔离，报 I/O error）。步骤 1、5 必须在**你自己的 Mac 终端**执行。
2. **VPS 可直连运维**：沙箱可用 paramiko + root 密码推 Nginx 配置、执行一次性命令。venv 路径 `/Users/zhy/.workbuddy/binaries/python/envs/vps/bin/python`，通用推送脚本 `push_nginx.py`。
3. **三处端口必须一致**：`vite.config.ts` 的 proxy、`frpc.toml` 的 `localPort`、`mhxy_nginx_new.conf` 的 `proxy_pass` 目标端口。
4. **location 顺序**：具体路径（`/api/dati`）必须排在兜底 `/api/` **之前**。
5. **VPS 带宽是瓶颈**：实测出口约 60KB/s。前端大文件（模型等）建议 int8 量化压缩，或后续上 CDN。
6. **Nginx 配置有备份**：`push_nginx.py` 每次自动打时间戳 `.bak`，可回滚。
7. **重启后自检**：`bash ~/mhxy-helper/restart_mhxy.sh` 会重启 frpc 并验证 watu/wuji 等。
